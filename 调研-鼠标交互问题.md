# Collections 菜单鼠标交互问题调研

## 现象

Collections 菜单中所有 UI 元素（列表、Back/Start 按钮）**只能用键盘交互**（方向键导航 + Enter 确认），**鼠标完全无法点击**。而游戏原生菜单（如 `LevelSelectMenu2`）鼠标交互正常。

## 核心原因分析

### 1. 键盘 vs 鼠标的输入路径差异（根本原因）

键盘和鼠标走的是两条完全不同的 Unity 输入路径：

| | 键盘 | 鼠标 |
|---|---|---|
| **选中方式** | `EventSystem.SetSelectedGameObject()` 直接指定 | `GraphicRaycaster` 射线检测命中 `Graphic` 组件 |
| **确认方式** | `ISubmitHandler.OnSubmit` 事件 | `IPointerClickHandler.OnPointerClick` → `Button.onClick` |
| **依赖** | `Selectable` + `Navigation` 组件 | `CanvasRenderer` 可见 + `raycastTarget = true` |
| **受 CanvasGroup.alpha 影响** | 否 | **是** — alpha=0 时 `CanvasRenderer.cull = true`，射线穿透 |

**关键点**：键盘导航完全绕过了 `GraphicRaycaster`，因此即使 UI 元素对射线不可见，键盘也能选中并操作它们。这就是"键盘能用、鼠标不能用"的根本原因。

### 2. `MenuTransition.Apply()` 的 alpha 动画导致 CanvasRenderer 被 cull

`MenuTransition.Apply()` (`sources/MenuTransition.cs:69-79`) 在 fade-in 动画期间操作 `CanvasGroup.alpha`：

```csharp
// FadeInForward 调用链: MenuSystem.cs:128-136
to.gameObject.SetActive(true);     // → OnEnable → BuildOnce 构建 UI
to.Transition(1f, 0f);             // → alpha = 1 - |1| = 0, z = 2000
to.Transition(0f, fadeInTime);     // → 动画: alpha 从 0 → 1, z 从 2000 → 0
to.OnGotFocus();                   // → 此时 alpha 仍为 0
```

**时序问题**：
1. `OnEnable()` 触发 `BuildOnce()`，此时 CanvasGroup.alpha = 1（刚创建），所有子元素的 `CanvasRenderer.cull = false`
2. `Transition(1f, 0f)` 立即将 alpha 设为 0，Unity 将子元素的 `CanvasRenderer` 标记为 `cull = true`
3. 0.3 秒动画将 alpha 从 0 渐变到 1
4. 动画结束后 `alpha = 1`，**但子元素的 `CanvasRenderer.cull` 可能未被正确重置**

在 Unity 2017.4 中，`CanvasGroup.alpha` 通过动画回到 1 后，子 `CanvasRenderer` 的 cull 状态有时不会自动恢复。这导致 `GraphicRaycaster` 永远找不到这些元素。

### 3. 为什么原生菜单不受影响

原生菜单（如 `LevelSelectMenu2`）是场景 Prefab，其 UI 元素在场景加载时就已存在。当它们被 fade-in 时：
- UI 结构在 alpha=1 的状态下已经稳定存在过
- `CanvasRenderer` 的初始状态是正确的
- alpha 动画后 cull 状态能正确恢复

而 `CollectionsMenu` 的 UI 全部在 `BuildOnce()` 中动态创建（`CollectionsMenu.cs:93-119`），创建发生在 `OnEnable` 期间。此时父级 `CanvasGroup` 虽然默认 alpha=1，但在同一帧内就被 `Transition(1f, 0f)` 改为 0。子元素的 `CanvasRenderer` 在一个不稳定的状态下被创建和 cull。

### 4. 次要因素：Z 轴偏移

`MenuTransition.Apply()` 还设置了 `localPosition.z = current * 2000f`。

如果游戏 Canvas 使用 **Screen Space - Camera** 模式（Human: Fall Flat 很可能使用此模式以支持 3D 菜单效果），Z 轴偏移会将菜单推到相机视锥体之外。动画期间 z 从 2000 渐变到 0，但如果动画未完成或 z 值残留，射线检测也会失败。

### 5. `InControlInputModule` 的鼠标处理

`InControlInputModule.Process()` (`sources/InControl/InControlInputModule.cs:167-184`)：

```csharp
if (!ProcessTouchEvents() && allowMouseInput)
{
    ProcessMouseEvent();
}
```

- `allowMouseInput` 默认为 `true`
- `ProcessMouseEvent()` 调用标准 Unity `GetMousePointerEventData()`，使用 `GraphicRaycaster` 进行射线检测
- `focusOnMouseHover` 控制 hover 时是否自动选中，不影响点击

`MenuSystem.FocusOnMouseOver(true)` 在 `MenuTransitionedIn` 时被调用（`MenuSystem.cs:138-141`），理论上应在动画完成后恢复鼠标功能。

## 相关文件及行号

| 文件 | 行号 | 说明 |
|------|------|------|
| `sources/MenuTransition.cs` | 23 | `private float phase = 1f;` — 初始 phase=1，OnEnable 不重置 |
| `sources/MenuTransition.cs` | 27-31 | `OnEnable()` — 仅缓存 RectTransform / CanvasGroup |
| `sources/MenuTransition.cs` | 33-48 | `Transition()` — 立即 alpha 切换 + 动画启动 |
| `sources/MenuTransition.cs` | 51-67 | `Update()` — 动画循环，phase<1 时调用 Apply() |
| `sources/MenuTransition.cs` | 69-79 | `Apply()` — **设置 alpha 和 Z 轴** |
| `sources/MenuSystem.cs` | 128-136 | `FadeInForward()` — 激活 + 设置动画 + 调用 OnGotFocus |
| `sources/MenuSystem.cs` | 138-141 | `MenuTransitionedIn()` — 动画完成回调，恢复鼠标 |
| `sources/MenuSystem.cs` | 185-189 | `FocusOnMouseOver()` — 设置 inputModule.focusOnMouseHover |
| `CollectionsMenu.cs` | 31-41 | `Awake()` — 创建 RectTransform + CanvasGroup |
| `CollectionsMenu.cs` | 43-54 | `OnEnable()` — 缓存组件 + 调用 BuildOnce |
| `CollectionsMenu.cs` | 56-81 | `OnGotFocus()` — 动画期间调用，alpha=0 时执行 |
| `CollectionsMenu.cs` | 93-119 | `BuildOnce()` — **动态构建所有 UI** |
| `CollectionsMenu.cs` | 180-252 | `BuildListView()` — 创建 ListView 及模板 |
| `CollectionsMenu.cs` | 314-349 | `BuildBackButton()` — 创建 Back 按钮 |
| `CollectionsMenu.cs` | 351-369 | `BuildStartButton()` — 创建 Start 按钮 |
| `sources/InControl/InControlInputModule.cs` | 32 | `allowMouseInput = true` |
| `sources/InControl/InControlInputModule.cs` | 34 | `focusOnMouseHover` |
| `sources/InControl/InControlInputModule.cs` | 167-184 | `Process()` — 鼠标事件入口 |
| `sources/InControl/InControlInputModule.cs` | 326-347 | `ProcessMouseEvent()` — 射线检测 + 点击处理 |

## 解决方案（优先级排序）

### 方案 A：将 UI 构建延迟到 `OnGotFocus` 之后（推荐 ⭐）

**原理**：在 fade-in 动画完成、alpha 恢复到 1 之后再创建 UI 元素，确保 `CanvasRenderer` 在正确的初始状态下创建。

**实现思路**：
- 将 `BuildOnce()` 的调用从 `OnEnable()` 移到 `OnGotFocus()` 中
- 首次 `OnGotFocus` 时构建 UI，后续调用只刷新数据
- 需要注意：`MenuTransition.OnEnable()` 需要 `RectTransform` 和 `CanvasGroup`，这两个仍需在 `Awake` 中创建

**优点**：从根本上避免了 CanvasRenderer cull 问题
**缺点**：需要处理 `OnGotFocus` 多次调用的情况；UI 构建从同步变为首次聚焦时

### 方案 B：在 `OnGotFocus` 中强制刷新 CanvasRenderer

**原理**：动画完成后强制所有子元素的 `CanvasRenderer` 重新计算 cull 状态。

**实现思路**：
```csharp
public override void OnGotFocus()
{
    base.OnGotFocus();
    // 强制刷新所有 CanvasRenderer 的 cull 状态
    foreach (var cr in GetComponentsInChildren<CanvasRenderer>(true))
    {
        cr.cull = false;  // 或: cr.SetColor(Color.white); 触发脏标记
    }
    // ... 其余逻辑
}
```

**优点**：改动最小
**缺点**：依赖于 Unity 内部行为，不同版本可能表现不同

### 方案 C：禁用 MenuTransition 的 alpha/Z 动画（不推荐）

**原理**：在 `CollectionsMenu` 中覆写 `Update()` 或让 `Apply()` 不操作 alpha/Z。

**缺点**：会丢失 fade-in 动画效果，与游戏其他菜单风格不一致

### 方案 D：延迟一帧后再构建 UI

**原理**：在 `OnEnable` 中使用 `StartCoroutine` 延迟一帧，等动画的第一帧 Apply 完成后再构建 UI。

**实现思路**：
```csharp
protected override void OnEnable()
{
    base.OnEnable();
    StartCoroutine(BuildDeferred());
}

private IEnumerator BuildDeferred()
{
    yield return null; // 等待一帧
    BuildOnce();
}
```

**优点**：改动较小
**缺点**：一帧延迟可能不够；如果动画第一帧已经设置了 alpha=0，结果同现在一样

### 方案 E：将 UI 元素放入独立的、不受动画影响的 Canvas

**原理**：CollectionsMenu 的核心交互 UI 放在一个独立子 Canvas 下，该 Canvas 有自己的 CanvasGroup 且不受父级动画影响。

**缺点**：引入额外复杂度，需要管理多个 Canvas

## 建议

**推荐方案 A**，理由：
1. 这是游戏原生菜单的实际模式：Prefab UI 在场景加载时就已存在，fade-in 只是显示/隐藏
2. CollectionsMenu 的 `OnGotFocus()` 已经在做回调绑定和数据刷新（`Rebuild()`），将 UI 构建移到这里是自然的扩展
3. 避免了与 Unity CanvasRenderer 内部行为的对抗

如果方案 A 因架构限制不可行，方案 B 是最简单的 fallback。
