# Collections 菜单列表组件方案调研

## 1. 背景

需求文档要求：

> 此处的列表使用游戏内置的多人游戏房间列表同样的样式。

当前 `CollectionsMenu` 使用自建的 `SimpleList` 内部类，试图模拟游戏原生列表的行为，但存在若干功能缺失。

## 2. 游戏原生列表架构

### 2.1 核心类

| 类 | 文件 | 职责 |
|---|---|---|
| `ListView` | `sources/ListView.cs:8` | 自定义可滚动列表。管理 item 实例化、滚动动画、轮播模式、EventSystem 交互。 |
| `ListViewItem` | `sources/ListViewItem.cs:4` | 列表项基类。处理 `ISelectHandler`/`ISubmitHandler`/`IPointerClickHandler` 等 Unity 事件，通过 `GetComponentInParent<ListView>()` 向上冒泡。 |
| `MenuButton` | `sources/MenuButton.cs:6` | 继承 `UnityEngine.UI.Button`，增加 `isOn` 切换状态和对应颜色过渡（选中时黑色背景白色文字，未选中时白色背景黑色文字）。 |
| `Ease` | `sources/Ease.cs:4` | 静态缓动函数工具类，`ListView` 依赖其 `easeOutQuad` 做平滑滚动动画。 |

### 2.2 多人游戏房间列表的使用方式

`MultiplayerSelectLobbyMenu`（`sources/Multiplayer/MultiplayerSelectLobbyMenu.cs:12`）：

```csharp
public ListView list;  // 在 Editor 中连接到 prefab 上的 ListView 组件

// OnGotFocus 中配置回调
list.onSelect = OnSelect;
list.onSubmit = OnSubmit;
list.onDeSelect = OnDeSelect;
list.onPointerClick = OnPointerClick;

// 绑定数据
list.Bind(items);  // items 是 List<NetTransport.ILobbyEntry>
```

`ListView` 的 item template 是 `MultiplayerSelectLobbyMenuItem`（`ListViewItem` 子类），其 `Bind(int index, object data)` 方法将 `ILobbyEntry` 数据绑定到 UI 文本。

### 2.3 `ListView` 的初始化（`Awake`）

`ListView.Awake()` 在运行时从以下字段读取配置（无需 prefab）：

- `itemTemplate`：`Button` — 带 `LayoutElement`（从 `preferredHeight`/`preferredWidth` 读取 item 尺寸）、带 `ListViewItem` 子类组件
- `itemContainer`：`GameObject` — 带 `HorizontalOrVerticalLayoutGroup`（从 `spacing` 读取间距，从布局类型判断水平/垂直方向）
- `scrollRect`：从 `GetComponent<RectTransform>()` 获取自身 RectTransform

## 3. `SimpleList`（当前实现）与 `ListView`（原生）对比

| 特性 | SimpleList（当前） | ListView（原生） |
|---|---|---|
| 滚动动画 | ❌ 无，瞬间跳转 | ✅ `Ease.easeOutQuad` 平滑过渡（0.25s） |
| 键盘/手柄导航 | ❌ 不支持 | ✅ Unity EventSystem 完整集成（`ISelectHandler`/`ISubmitHandler`） |
| 鼠标悬停选择 | ❌ 不支持 | ✅ `IPointerEnterHandler` → 自动 `OnSelect` |
| 鼠标滚轮滚动 | ❌ 不支持 | ✅ `IScrollHandler` → `OnScroll` |
| 轮播模式 | ❌ 不支持 | ✅ 当 item 数超过可见区域时自动启用循环 |
| 选中高亮 | ⚠️ 手动设置 `Image.color`，无 `MenuButton.isOn` 状态 | ✅ `MenuButton.isOn` 原生的黑底白字/白底黑字过渡 |
| 自动布局 | ⚠️ 手动 `VerticalLayoutGroup` + `ContentSizeFitter` | ✅ `HorizontalOrVerticalLayoutGroup` |
| 与游戏风格一致性 | ⚠️ 部分一致（克隆了模板的外观，但缺少交互动画） | ✅ 100% 一致 |

**SimpleList 缺失的最关键功能**：键盘/手柄导航。Human: Fall Flat 在主机平台上完全依赖手柄操作，没有 EventSystem 集成意味着手柄用户无法在列表中导航。

## 4. 结论：可以直接使用 `ListView`

### 4.1 可行性

**`ListView` 完全可以在纯代码中构建。** 它是普通的 `MonoBehaviour`，不依赖 `.prefab` 文件。所有字段（`itemTemplate`、`itemContainer`）在运行时设置即可。

证据：
- `ListView.Awake()` 仅从已设置的 `itemTemplate` 和 `itemContainer` 读取配置
- 游戏中的 `MultiplayerSelectLobbyMenu` 和 `LevelSelectMenu2` 在 Editor 中连线这些字段，但运行时动态设置同样有效
- `ListView` 依赖的 `Ease` 是游戏内置的静态工具类，始终可用

### 4.2 需要新增的组件

需要一个自定义的 `ListViewItem` 子类用于集合列表项：

```csharp
// 新文件: CollectionListItem.cs (~30行)
public class CollectionListItem : ListViewItem
{
    public TextMeshProUGUI label;
    
    public override void Bind(int index, object data)
    {
        base.Bind(index, data);
        label.text = data as string ?? "";
    }
}
```

### 4.3 需要修改的文件

仅 `CollectionsMenu.cs`：
- 移除 `SimpleList` 内部类（约 160 行）
- 重写 `BuildColList()` 和 `BuildLvlList()`，构建 `ListView` 替代 `SimpleList`
- 重写数据绑定逻辑，从 `SimpleList.Bind(List<string>)` 改为 `ListView.Bind(IList)` + `onSelect` 回调

### 4.4 构建 `ListView` 的关键步骤

```csharp
// 1. 创建 ListView GameObject（先 inactive 或先配置字段再 AddComponent）
var listGo = new GameObject("ListView", typeof(RectTransform));
listGo.SetActive(false);  // 防止 AddComponent 时 Awake 立即执行

// 2. 创建 itemContainer
var container = new GameObject("ItemContainer", typeof(RectTransform));
container.transform.SetParent(listGo.transform, false);
container.AddComponent<VerticalLayoutGroup>().spacing = 2f;

// 3. 创建 itemTemplate（inactive 的 Button + ListViewItem + LayoutElement + MenuButton）
var template = new GameObject("ItemTemplate", typeof(RectTransform));
template.transform.SetParent(listGo.transform, false);
template.AddComponent<LayoutElement>().preferredHeight = 40f;
template.AddComponent<MenuButton>();  // 替代普通 Button，提供 isOn 视觉状态
var item = template.AddComponent<CollectionListItem>();
// ... 添加 label 子对象
template.SetActive(false);  // ListView.Awake 也会做这一步

// 4. 添加 ListView 组件并配置
var lv = listGo.AddComponent<ListView>();
lv.itemTemplate = template.GetComponent<Button>();
lv.itemContainer = container;

// 5. 激活
listGo.SetActive(true);
```

### 4.5 注意事项

1. **`AddComponent` 时序**：`ListView.Awake()` 在 `AddComponent<ListView>()` 时立即调用（如果 GameObject 是 active 的）。必须先设置好 `itemTemplate` 和 `itemContainer` 再激活，或者先 inactive → AddComponent → 设置字段 → 再 active。

2. **`MenuButton.isOn` 状态管理**：`ListView` 本身不管理 `isOn`。需要在 `onSelect`/`onDeSelect` 回调中手动设置，参考 `MultiplayerSelectLobbyMenu.OnSelect()` 的做法。

3. **EventSystem 依赖**：`ListView` 依赖场景中的 `EventSystem`。游戏场景中已有，无需额外处理。

4. **数据绑定**：`ListView.Bind(IList list)` 接受非泛型 `IList`，每个元素通过 `ListViewItem.Bind(int index, object data)` 传递。我们的数据是 `List<string>`，可以直接传入。

5. **垂直方向限定**：`ListView` 通过 `itemContainer` 上的 `HorizontalOrVerticalLayoutGroup` 自动检测方向。我们使用 `VerticalLayoutGroup`。

## 5. 替代方案

如果因为某些原因无法使用 `ListView`，替代方案是**增强 `SimpleList`** 使其更接近原生行为：

| 需要添加的功能 | 复杂度 |
|---|---|
| Unity EventSystem 导航（`ISelectHandler` 等） | 中 — 需要实现多个接口 |
| 平滑滚动动画 | 低 — 手动插值 `anchoredPosition` |
| 鼠标滚轮支持 | 低 — `IScrollHandler` |
| `MenuButton.isOn` 状态管理 | 低 — 切换 `isOn` |
| 轮播模式 | 高 — 需要 clone 额外 item 并处理循环 |

这个方案工作量大且最终效果仍不如直接使用 `ListView`，**不推荐**。

## 6. 总结与建议

| 方案 | 推荐度 | 工作量 | 最终效果 |
|---|---|---|---|
| **A. 直接使用 `ListView`** | ⭐⭐⭐ 强烈推荐 | ~1-2 小时 | 与原生 100% 一致 |
| B. 增强 `SimpleList` | ⭐ 不推荐 | ~4-6 小时 | 近似但不完全一致 |

**建议采用方案 A**。核心原因：

- `ListView` 是游戏的标准列表组件，使用它能保证与多人游戏房间列表**完全一致**的交互体验
- 键盘/手柄导航是刚需（游戏支持主机/手柄），`SimpleList` 完全不支持
- 工作量更小，代码更简洁（移除 ~160 行 `SimpleList`，新增 ~80 行 `ListView` 构建 + ~30 行 `CollectionListItem`）
